import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   takePhoto: {
      width: 60,
      height: 60,
      borderRadius: 30,
      position: 'absolute',
      bottom: 20,
   },
});

export default styles;
