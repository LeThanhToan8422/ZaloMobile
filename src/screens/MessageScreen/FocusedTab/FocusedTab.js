import { View, Text } from 'react-native';
import React from 'react';
import ListChat from '../../../components/ListChat';

export const FocusedTab = ({ navigation }) => {
   return <ListChat navigation={navigation} />;
};
