import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   container: {
      width: '80%',
      height: 200,
      alignSelf: 'center',
   },
   codeInput: {
      width: 30,
      height: 45,
      borderWidth: 0,
      borderBottomWidth: 1,
      borderColor: '#666',
      color: '#000',
   },
});

export default styles;
