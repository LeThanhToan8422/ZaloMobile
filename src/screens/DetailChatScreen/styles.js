import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   avatar: {
      alignSelf: 'center',
      margin: 12,
      width: 80,
      height: 80,
      borderRadius: 40,
   },
   name: {
      marginTop: 12,
      fontSize: 20,
      alignSelf: 'center',
   },
});

export default styles;
